{
  "name": "chris-test-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon ./bin/www",
    "start": "concurrently \"npm install && nodemon ./bin/www\" \"cd client && npm install && npm start \""
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "dotenv": "^11.0.0",
    "express": "^4.17.2"
  }
}
